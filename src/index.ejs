<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Widget Template</title>

    <style>
      .preview {
        position: relative;
        width: 60px;
        height: 100%;
      }

      .preview-widget {
        position: absolute;
        top: 50px;
        right: 50px;
      }
    </style>
  </head>

  <body>
    <div data-widget-host="habitat" class="preview"></div>
    <div data-widget-host="habitat_widget" class="preview-widget"></div>
  </body>
</html>
